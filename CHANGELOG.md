## 更新记录

### version: 3.3.8：

武林争霸改为武林争霸赛；修复只写阵营（恶人/恶人谷、浩气/浩气盟）时被误认为区服导致总是返回浩气盟的问题。武林争霸赛图片标题改为“武林争霸赛 · 阵营”，并增加“排行榜仅展示前 50 名”小字，结果只保留前 50 名。

认领改为多人制；每个会话的认领人是第一个绑定成功的人，后续绑定不再覆盖。页面取消认领资格会移除该认领人在所有会话的记录。AstrBot 管理员绑定成功后也记录真实用户身份。

### version: 3.3.7：

新增授权管理、查看管理、删除管理命令；认领人固定排在第一位，授权管理员可管理通知开关与查询令牌，AstrBot 管理员不列入列表。Token、推栏标识改为任意用户私聊提交后真实校验入库，过期或次数不足时返回明确提醒。插件页面新增认领人昵称与授权管理员编辑栏，会话列表按群名或私聊昵称展示。

### version: 3.3.6：

接入 JX3API 事件通道，主动通知改走 WebSocket。新增通知管理出图查看本群事件开关。新增全局命令页和区服别名页。命令改名全局生效。写出的区服按正式名和别名识别，无法识别时提示输入正确的区服。命令参数错误改为返回正确格式。移除骗子查询和本地避雷。通知管理图补上赤兔。

移除副本记录命令，JX3API 当前无对应接口。

命令与描述改为同一事务写入；TLS 校验默认开启并新增 jx3api_ssl_verify 配置。

### version: 3.3.5：

移除名片预设。名片文字改为区服-角色与点赞分行；全名片状态汇总为一行后再发图。

### version: 3.3.4：

补全 JX3API 缺失接口：跨服名剑、武林争霸、捕快、浪客、决斗、资历分布、外观搜索、名片预设。

沙盘、攻略、配装改回原数据源；物价仍走原物品价格接口。Token 过期或次数用尽会给出明确提示。帮助页命令与免费/付费标记分行显示。

### version: 3.3.3：

插件会话卡片改为三行对齐：区服、JX3API Token、推栏标识各占一行，并支持清除区服。

页面去掉开服/新闻/刷马/赤兔按钮；使用全局 JX3API Token 与启用机器人放到卡片顶部。

### version: 3.3.2：

修复插件页面保存失效：勾选全局 Token、开关推送、填写密钥可真正写入当前会话。

页面只显示已绑定区服的群/私聊，并支持开关该会话机器人；关闭后查询、管理和推送都不再进入该会话。

帮助页按通用、休闲、秘境、阵营重新分组。

### version: 3.3.1：

查询结果图改为浅色纸纹背景，帮助页为每条指令标注免费 / 需 Token / 需 Token+推栏。

插件会话页改为卡片列表，勾选「使用全局 Token」立即保存；Token 提示不再暴露真实 UMO。

JX3API 购买链接统一为 https://www.jx3api.com ，去掉尾斜杠。

### version: 3.3.0：

按群/私聊绑定区服，查询未写区服时使用当前会话绑定服。

主动推送改为 打开 新闻、关闭 开服 等命令，按会话隔离发送。

JX3API Token 按会话配置，未勾选使用全局则不可用付费功能；推栏默认走全局，会话自定义优先。

新增插件页面查看绑定状态，配置页移除默认服务器和四类推送配置。

### version: 3.2.3：

API区服接口功能调整，做对应调整

### version: 3.2.2：

调整部分功能指令的描述

### version: 3.2.1：

统一封装 JX3BOX Node、Next2 和 CMS 三类接口请求，集中处理基础地址、GET/POST、请求参数、返回字段提取与异常日志。

修复资历菜单和点数缓存的刷新链路，缓存过期后分别请求 `/api/node/achievement/menus` 与 `/api/node/achievement/points`，上游失败时继续使用旧缓存。

刷马与赤兔后台推送改用 JX3BOX Next2 马场消息接口，并向后台任务管理器注入 `JX3BOXService`。

配装查询开始实际传递类型筛选参数。

新增项目级 `.gitignore`，忽略 Python 缓存、虚拟环境、日志、临时文件及整个 `tests/` 目录。

同步更新 README、插件注册信息和元数据版本号。

### version: 3.1：

增加副本记录查询功能。

重构 46 个 HTML 图片模板：公共文档骨架、设计变量、基础样式和组件样式统一维护，页面仅保留内容与独有布局；渲染前在本地组合为完整 HTML 字符串。

进一步组件化列表和卡片页面：表格、排行、技能/奇穴、奇遇网格、区服状态、器物详情等改为复用公共 CSS；页面专属 CSS 从 46 个减少至 13 个。模板通过元数据声明组件，加载器从单一组件样式表中选择性注入所需样式，并支持无同名 CSS 的标准页面及共享资源缓存。

重新整理功能帮助页，按 17 个分类展示 69 项功能和 105 条指令，并按固定图片画布优化三列排版。

### version: 3.0：

大部分功能重写，还有部分未写完。

### version: 2.7：

重构 **交易行 物品名称 [服务器]** 指令，改为免令牌图片查询。

新增 JX3BOX 交易行物品库缓存，用户输入物品名后先在本地模糊匹配物品 ID，再批量请求交易行价格接口。

交易行结果图改为列表展示，包含物品图标与名称、砖金银铜图标化价格、数量和数据时间。

价格接口未返回的物品不再展示，全部无价格数据时返回明确提示。

### version: 2.6：

新增 **资历 角色名称 [服务器]** 指令，支持两轮对话选择资历总览或分类总览，并以图片形式展示角色资历点数完成进度。

新增资历菜单与点数基础数据缓存，减少长期稳定接口的重复请求。

调整功能帮助页分类，指令分组改为日常、官方、角色、职业、休闲、阵营、副本、活动、交易、杂项、排行、推送和避雷。

调整指令触发词：**诛恶事件** 改为 **诛恶**，**排行** 改为 **名剑排行**。

### version: 2.5：

增加部分新指令，调整功能页面

### version: 2.4：

增加大量新指令

### version: 2.3：

宏查询接口调整，恢复使用

### version: 2.2：

接口地址调整,增加部分功能

### version: 2.1：

接口地址调整

### version: 2.0：

接口地址调整

### version: 1.2.4：

角色信息查询数据展示优化

角色名片查询功能优化

### version: 1.2.3：

查询个人奇遇的指令名称调整为 **查询**

新增角色基本信息查询功能 **角色 名称 [服务器]**

### version: 1.2.2：

调整角色名片查询功能。

### version: 1.2.1：

调整角色名片查询功能。

### version: 1.2.0：

部分HTML渲染图片功能优化，增加本地图片加载功能。

### version: 1.1.1：

图片渲染参数优化。

### version: 1.1.0：

部分代码功能重写。

### version: 1.0.0：

大部分重要功能已经实现
